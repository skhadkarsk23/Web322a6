<!DOCTYPE html>
<html lang="en" data-theme="cupcake">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= set.name %> - Lego Set Details</title>
    <link href="/css/main.css" rel="stylesheet" />
  </head>
  <body class="bg-base-100 text-base-content">
    <!-- Updated Navbar -->
    <%- include('./partials/navbar', { page: 'sets' }) %>

    <div class="hero bg-base-200 rounded-lg p-6 mb-8">
      <div class="hero-content text-center">
        <h1 class="text-5xl font-bold"><%= set.name %></h1>
        <p class="py-4">Here are the details for the Lego set: <strong><%= set.name %></strong></p>
      </div>
    </div>

    <div class="container mx-auto p-6">
      <div class="card bg-base-200 shadow-lg">
        <img src="<%= set.img_url %>" alt="Image of <%= set.name %>" class="w-48 h-48 object-cover mx-auto rounded-lg" />
        <div class="card-body text-center">
          <h2 class="card-title"><%= set.name %></h2>
          <p><strong>Year:</strong> <%= set.year %></p>
          <p><strong>Theme:</strong> <%= set.theme %></p>
          <p><strong>Number of Parts:</strong> <%= set.num_parts %></p>

          <div class="card-actions justify-center mt-4">
            <a href="/lego/editSet/<%= set.set_num %>" class="btn btn-success">Edit</a>
            <a href="/lego/deleteSet/<%= set.set_num %>" class="btn btn-error">Delete</a>
          </div>
        </div>
      </div>
    </div>

    <div class="text-center mt-8">
      <a href="#" onclick="history.back(); return false;" class="btn btn-primary">Go Back</a>
    </div>
  </body>
</html>
